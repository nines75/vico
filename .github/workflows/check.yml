name: Check

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  check:
    name: ${{ matrix.task }}
    timeout-minutes: 30
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        task: ["build", "compile", "lint", "format:ci"]
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Setup pnpm
        uses: pnpm/action-setup@0977fd99725f1db4007ccb2928dbb4e90d06cc86 # v6.0.10
        with:
          run_install: true
          cache: true

      - run: pnpm ${{ matrix.task }}
